# PHY Knowledge Distillation

See **[phy/README.md](phy/README.md)** for full documentation, including:

- Project overview and paper abstract
- MATLAB PHY simulation setup (teacher data generation)
- Python PKD student model architecture and usage
- Runtime performance results
- Citation

## Quick Start

```bash
# Install Python dependencies
pip install -r requirements.txt

# Run tests
cd pkd/tests && python test_components.py

# Run example
python -m pkd.example
```
